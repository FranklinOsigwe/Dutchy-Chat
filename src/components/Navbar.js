import { Box } from '@mui/material';
import {AppBar,Toolbar} from '@mui/material'

function Navbar() {
  return (
    <AppBar position ='stick'><Toolbar>Navbar</Toolbar></AppBar>
  )
}

export default Navbar